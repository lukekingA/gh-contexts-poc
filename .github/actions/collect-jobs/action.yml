name: collect-jobs
description: Collects all jobs in a workflow

runs:
  using: composite
  steps:
    - name: Collect jobs
      shell: bash
      env:
        JOBS: ${{ env.JOBS }}
      run: |
        if [ -z "$JOBS" ]; then \
          echo JOBS >> $GITHUB_ENV; \
        fi
        